<UserControl x:Class="Decrypta.App.Views.DoctorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml">
  <Grid Margin="28,20,28,20">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
    </Grid.RowDefinitions>

    <StackPanel Grid.Row="0">
      <TextBlock Text="Doctor" Style="{StaticResource Text.Title}"/>
      <TextBlock Style="{StaticResource Text.Subtitle}" Margin="0,6,0,0"
                 Text="Checks the whole chain end to end: bundled tools, Apple Mobile Device Service, your device, the USB→SSH tunnel, and sign-in state."/>
    </StackPanel>

    <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,16,0,0">
      <ui:Button Content="Run checks" Appearance="Primary" MinWidth="130" Command="{Binding RunDoctorCommand}"/>
      <ProgressBar Width="200" Height="4" Margin="18,0,0,0" VerticalAlignment="Center"
                   IsIndeterminate="{Binding ProgressActive}"
                   Visibility="{Binding ProgressActive, Converter={StaticResource BoolToVis}}"/>
    </StackPanel>

    <ui:Card Grid.Row="2" Margin="0,14,0,0" VerticalContentAlignment="Stretch" Padding="8">
      <ScrollViewer VerticalScrollBarVisibility="Auto">
        <ItemsControl ItemsSource="{Binding DoctorRows}">
          <ItemsControl.ItemTemplate>
            <DataTemplate>
              <Border Background="{StaticResource Brush.Inset}" CornerRadius="8" Padding="14,11" Margin="2,4">
                <Grid>
                  <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="62"/>
                    <ColumnDefinition Width="150"/>
                    <ColumnDefinition Width="*"/>
                  </Grid.ColumnDefinitions>
                  <Border Grid.Column="0" CornerRadius="6" Padding="7,2" HorizontalAlignment="Left"
                          VerticalAlignment="Center" Background="{Binding Status, Converter={StaticResource CheckBrush}}">
                    <TextBlock Text="{Binding Status, Converter={StaticResource CheckLabel}}"
                               Foreground="#0A0A0F" FontSize="10.5" FontWeight="Bold"/>
                  </Border>
                  <TextBlock Grid.Column="1" Text="{Binding Name}" FontWeight="SemiBold" FontSize="13"
                             Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                             VerticalAlignment="Center" Margin="10,0,0,0"/>
                  <TextBlock Grid.Column="2" Text="{Binding Detail}" FontSize="12.5"
                             Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                             VerticalAlignment="Center" TextWrapping="Wrap"/>
                </Grid>
              </Border>
            </DataTemplate>
          </ItemsControl.ItemTemplate>
        </ItemsControl>
      </ScrollViewer>
    </ui:Card>
  </Grid>
</UserControl>
