# Build output
[Bb]in/
[Oo]bj/
publish/
publish_cli/
release/

# Rider/VS
.vs/
*.user

# Local scratch
_shots.ps1
_nettest.ps1
_doctor.ps1
_commitmsg.txt
_spike/

# Decrypta per-user state is under %LOCALAPPDATA%, never in-repo — but ignore any stray copy.
.state/

# Never commit credentials, decrypted output, or App Store keychains — belt and suspenders.
**/ipadecrypt/config.json
cookies/
out/
*.ipa

# Other tools
remove_black_corners.py
iOS-Runtime/
# NOTE: tools/ipadecrypt.exe IS committed on purpose: the app bundles it and the build copies it
# next to the output. It comes from the upstream MIT release.
